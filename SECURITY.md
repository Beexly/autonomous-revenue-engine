# Security — operational, not the GitHub template

Copilot: this file was a placeholder. Security has veto. Here is the veto.

## Vetoes (machine-checkable in `docs/signal-origin/protocol/policy.ts`)

1. No spend. No paid API, GPU, cluster, broker, ads.
2. No publish, send, merge, deploy from an agent.
3. Sports never enters this repo or the bus. GSE is a different login.
4. Adult never enters this repo. Beexly/XXX, local, fenced.
5. Secrets never commit. Supabase keys in `docs/js/config.js` stay placeholders until owner fills them.
6. Auto-post, engagement pods, cloned faces from Pinterest: kill.
7. Temporal / Redis / Langfuse / Airbyte / a second desk: kill. They look like security and become a surface.

## Incidents

If a draft that used a banned stencil (SO-013: "We killed three posts") is in the pass queue, it is not a pass. Kill it in place.

If a lead form shows success on a failed insert, that is silent lead loss. Honest mailto until keys exist.

## Reporting

Owner only: `signal.origin.hq@gmail.com`. No public bounty. No bot that files issues on a CVE scanner we don't run.

## Supported

This branch. Main is not written from this seat.
