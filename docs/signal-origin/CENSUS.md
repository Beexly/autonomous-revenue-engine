# Census — tonight's repos, honest depth

Not 150 clones. Unique names from the pastes + Copilot + the 15+15 lists: **112**.
Time spent in tables is not time spent in their source. This file says which is which.

## Already live in ARE (we ignored these for too long)

| App | Steal from | Now on the desk |
|---|---|---|
| `apps/qi-check` | Hold 9.2 | Pulse |
| `apps/swap-check` | QUALITY_BAR swap | Pulse + gold |
| `apps/subject-fold` | Email fold | Kit gold subject, this hour |
| `apps/voice-delta` | Burrows' Delta | Still CLI. Desk uses pressure field as the cheap cousin. Delta stays the library when a Pass corpus exists. |
| `apps/kit` + `docs/kit/` | Walk-in | Kit tab |
| `apps/conformal-lite` | Prediction sets | Parked. No series. |

## Coded this session (the idea runs)

MCP envelope, VERIFY, route, handoff, policy, cost, durable, search, trace, funnel, studio, qc, note, reuse, continuity, shot, calendar, math, packet, fold, timeline, query.

## Table-only on purpose (neighbor holds)

LangChain, LangGraph, AutoGPT, babyagi, OpenHands, aider, nanobot, AstrBot, agent-zero, camel, SuperAGI, langroid, llama_deploy, Temporal, Redis, Kafka, Rabbit, NATS, Argo, Prefect, Dagster, Kestra, Airbyte, Litestar, Reflex, Poetry, Pixi, Qdrant, Chroma, Milvus, Weaviate, mem0, Attu, neurons, DSPy, Berkeley FC, TruLens, UpTrain, llm-guard, LiteLLM, n8n, Activepieces, Node-RED, Appsmith, Directus, PostHog, Metabase, Plausible, Matomo, Cal.com, Chatwoot, MindsDB, Merlion, ERPNext, open-saas, SaaS-Boilerplate, stripe starters, Payload, Strapi, Medusa, Framer Motion, Lenis, Locomotive, Anime, MCP servers zoo, python MCP SDK, Semantic Kernel, OPA-the-server, OpenLineage, Marquez, GrowthBook, Langfuse-the-host.

Each of those has a row in [REPLACE.md](./REPLACE.md) or [STACK.md](./STACK.md). If REPLACE is missing a name, that is a bug — add a neighbor, don't re-install.

## Still shallow (owed a deeper steal, not a host)

| Name | Why shallow | Next steal without a bill |
|---|---|---|
| obra/superpowers | Skills named, not copied | verification-before-completion is policy + tests. TDD is `*.test.ts`. Don't paste their "ask first." |
| elizaOS character | Schema exists | Rails JSON is the character file. Don't boot their OS. |
| mastra evals | Suspend = owner | Already envelope. |
| pipecat | Parked as "voice later" | Kit `tel:` is the voice path. Realtime stack still a bill. |
| voice-delta | CLI only | Add two more Pass-only refs before a desk widget. One sample is not σ. |
| conformal-lite | numpy pin | Needs a real metric series. Measurement has five rows. Not enough. |
| twitter/communitynotes | Draft note only | Owner keys on the approval list. |
| promptfoo | Fixtures exist, CLI not run | Pressure tests in `src/lib/*.test.ts` are the CLI. |
| Theatre.js | Timeline beats tonight | Night tab. No npm. |
| duckdb / datasette | query.ts | Five rows. Grow the CSV, not the warehouse. |

## Integrity

I did not read 112 codebases line by line in twenty minutes. I filtered them against $0, no publish, git-as-bus, cash-this-week. Where a filter was a shrug, REPLACE now names the neighbor and a file that runs.
