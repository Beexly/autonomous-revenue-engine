# Stack decisions — 2026-09-14 night

Scored against Origin constraints: **$0, no publish, no new runtime, ARE + bus, cash this week, do not regress Kit/Hold.**

Doctrine already on the bus: BUILD-BIBLE owns Theatre.js / GSAP / three / r3f / Spark for the immersive factory. That factory is **parked until first cash.** This file is the Signal Origin overlay: what we steal *now*, what we leave in the bible, what we kill so it cannot eat the week.

Legend: **STEAL** = copy the idea into markdown/JSON this week. **BIBLE** = already specified for the parked 3D factory. **KILL** = do not install, do not self-host, do not open a second company.

---

## 0. The rule that stops cargo-cult

Installing LangGraph because we have two agents is how you get a third agent to babysit the framework. The bus already moves tasks. Pressure already scores drafts. Kit already is a page. A new runtime that does not raise a dollar or a Hold score is a regression.

---

## 1. Motion / 3D

| Repo | Verdict | Why |
|---|---|---|
| mrdoob/three.js | BIBLE | FrameFit + Spark + parked factory. **Never on a Kit walk-in.** WebGL on a call-button page is a conversion leak. |
| pmndrs/react-three-fiber | BIBLE | React path for the factory. Not the SO desk. |
| pmndrs/drei | BIBLE | Helpers when the factory wakes. |
| pmndrs/react-postprocessing | BIBLE | After a scene exists. |
| greensock/GSAP | BIBLE | Interaction. Bible already split GSAP vs Theatre.js. |
| theatre-js/theatre | BIBLE + STEAL | Directed timeline. Steal: Night log is a timeline of pastes, not a GSAP tween. Do not npm install Theatre for three replies. |
| framer/motion | KILL (SO) | Desk already has CSS tokens (`--motion-fast`). Extra lib is weight on a 440px phone. |
| darkroomengineering/lenis | KILL | Smooth scroll fights tap-to-call. Luxury, not a shop floor. |
| locomotivetml/locomotive-scroll | KILL | Same. |
| juliangarnier/anime | KILL | Redundant with GSAP/CSS. |

**Fork this week:** Pressure field is a 2D SVG of paragraph widths (the even-pressure tell). Spatializes the product without three.js.

---

## 2. Agent runtimes (do not nest a second Origin)

| Repo | Verdict | Why |
|---|---|---|
| Significant-Gravitas/AutoGPT | KILL | Recursive loops + tool use without a publish fence. Origin's fence is the product. |
| yoheinakajima/babyagi | KILL | Task soup. We have TASK-###. |
| All-Hands-AI/OpenHands | KILL | We *are* the coding agent. Nesting OpenHands is a hall of mirrors. |
| Aider-AI/aider | KILL | Same. |
| microsoft/autogen | STEAL | Role messages. Already Motif / Grok / Distro. Do not pip install. |
| langchain-ai/langgraph | KILL | Graph runtime for a two-agent markdown bus is theatre. |
| crewAIInc/crewAI | STEAL | "One crew per job." GROK_BOT_X_RD motion 2 already: one group chat per post. |
| microsoft/semantic-kernel | KILL | C# / plugin host. Wrong shop. |
| openai/swarm | STEAL | Handoff is a file in `inbox/`. That's swarm. |
| run-llama/llama_deploy | KILL | Deploy plane we do not have. |
| deepset-ai/haystack | KILL | RAG pipelines. Our corpus is git grep. |
| THUDM/AgentBench | STEAL | Eval suite idea → `eval/pressure.fixtures.json`. |
| geekan/MetaGPT | STEAL | Role cards (PM/Eng/QA) → Motif=PM+QC, Grok=Eng, Distro=writer. File: `protocol/roles.json`. |
| camel-ai/camel | KILL | Society sim. Two agents is not a society. |
| TransformerOptimus/SuperAGI | KILL | Toolchain sprawl. |
| microsoft/JARVIS | STEAL | LLM as controller over experts. Motif already routes. |
| langroid/langroid | KILL | Extra Python chat runtime. |
| letta-ai/letta | STEAL | Core memory vs archival. Desk Intel = core. Origin-box 112 files = archival. Do not host Letta. |
---

## 2.5 Fifteen runtimes (2026-09-14 late)

Same rule. Stars are not a reason to nest a second Origin.

| Repo | Stars | Verdict | Steal | Kill |
|---|---|---|---|---|
| langchain-ai/langchain | 146k | KILL | Integrations list as reading | The framework. Two agents don't need LCEL. |
| obra/superpowers | 286k | STEAL | Skills as markdown gates: TDD, verify-before-done, finish = PR not merge. | Their marketplace, MCP wrapper, "ask before every feature" (anti-stall). |
| FoundationAgents/MetaGPT | 70k | STEAL | Role cards. Already `roles.json`. | The software-company sim. |
| microsoft/autogen | 60k | KILL as runtime | Roles. | Maintenance mode. Successor is agent-framework. Don't migrate into Azure. |
| crewAIInc/crewAI | 58k | STEAL | One crew per job. | The runtime. |
| langchain-ai/langgraph | 41k | KILL | Explicit state. We have a packet. | Graph server. |
| HKUDS/nanobot | 48k | KILL | One workspace per project (ARE / bus / XXX). | Telegram, cron, image gen, a second WebUI. The desk is the UI. |
| AstrBotDevs/AstrBot | 40k | KILL | — | IM auto-send. Fence exists so we don't. |
| openai/openai-agents-python | 29k | STEAL | Handoff as `transfer_to_*`. Swarm is dead; this is the successor. Git inbox is the handoff. Guardrails = VERIFY. | pip install. Sandbox agents. |
| mastra-ai/mastra | 28k | STEAL | Zod in, Zod out. Suspend = owner. `.then()` is a task file. | npm create mastra. Supervisor agents. |
| deepset-ai/haystack | 26k | KILL | — | RAG. git grep. |
| elizaOS/eliza | 19k | STEAL | Character file = face/motion/calendar. One character per rail. | Booting a Linux distro for a $350 walk-in. |
| agent0ai/agent-zero | 19k | KILL | — | Autonomous execute without a publish fence. |
| pipecat-ai/pipecat | 15k | PARK | Voice later. | Not this week. |
| microsoft/agent-framework | 13k | STEAL | HITL, YAML agent, MCP annotations (already stolen). | pip install, Azure SSO, a third Microsoft runtime. |

**This week, in code:** `protocol/handoff.ts` (OpenAI handoff, git folders). `protocol/skills.md` (superpowers without the stall). Eliza already lives in `factory/*.json`.

---


## 3. Protocol (the one that actually upgrades the bus)

| Repo | Verdict | Why |
|---|---|---|
| modelcontextprotocol/typescript-sdk | STEAL | Tools / resources / prompts + annotations (`readOnlyHint`, `destructiveHint`). Do not run an MCP server. Git *is* the transport. |
| modelcontextprotocol/python-sdk | KILL | Duplicate of the schema. |
| modelcontextprotocol/servers | KILL | Third-party server zoo. Approval-gated tools only, listed in `protocol/tools.json`. |
| microsoft/semantic-kernel | KILL | See above. |

**Fork this week:** `protocol/envelope.schema.json` — every bus task is a tool call with MCP annotations. `destructiveHint` + `openWorldHint` ⇒ approval list, never silent send.

---

## 4. Durability / transport (already solved)

| Repo | Verdict | Why |
|---|---|---|
| temporalio/temporal + sdk-typescript | KILL | Markdown + git is the durable log. Temporal is a company. |
| RabbitMQ/rabbitmq-server | KILL | We are not a queue. |
| apache/kafka | KILL | Same, louder. |
| nats-io/nats-server | KILL | Same. |
| PrefectHQ/prefect | KILL | Overnight is three pastes, not a DAG. |
| dagster-io/dagster | KILL | Asset graph for a $350 walk-in is a joke. |
| kestra-io/kestra | KILL | Same. |

---

## 5. Memory / retrieval

| Repo | Verdict | Why |
|---|---|---|
| getzep/graphiti | STEAL | Episode + `valid_at` / `invalid_at`. JSON, no Neo4j. `memory/episodes.json`. |
| getzep/zep | STEAL | Long-term vs session. Origin-box vs tonight. |
| mem0ai/mem0 | KILL | Hosted memory. Git is memory. |
| qdrant / chroma / milvus / weaviate | KILL | Vector DBs for 112 markdown files is malpractice. |
| redis/redis-vl-python | KILL | Redis is a bill. |
| zilliztech/attu | KILL | GUI for a DB we will not run. |
| NeumTry/neurons | KILL | Experiment. Not cash. |

**Fork this week:** episodes for James Howard OOO, 150-row unverified, X=Sports, dual-gate, Kit hunts.

---

## 6. Eval / observability / rails

| Repo | Verdict | Why |
|---|---|---|
| promptfoo/promptfoo | STEAL | Fixture file. Pressure is the scorer. No CLI, no provider keys. |
| langchain-ai/openevals | STEAL | Assertion types: contains / notContains / score. |
| instructor-ai/instructor | STEAL | Structured outputs. Desk already uses typed results. |
| stanfordnlp/dspy | KILL | Optimizer we cannot feed. |
| gorilla-llm/berkeley-function-call-leaderboard | KILL | Not our bench. |
| truera/trulens | KILL | Extra eval runtime. |
| uptrain-ai/uptrain | KILL | Same. |
| NVIDIA/NeMo-Guardrails | STEAL | "Never publish / never spend" as a rail. Already a charter. Encode in envelope. |
| protectai/llm-guard | KILL | Overkill for paste-ready replies. |
| langfuse / Arize phoenix / Helicone / openlit / langwatch / braintrust | KILL | Observability for an LLM API we are not paying. Git log is the trace. |
| BerriAI/litellm | KILL | One provider: Grok. A gateway is a second bill. |

**Fork this week:** `eval/pressure.fixtures.json` — RB-02 Hold on reply, slogan stack Hard, brochure verbs fail.

---

## 7. Automation / no-code / growth

| Repo | Verdict | Why |
|---|---|---|
| n8n-io/n8n | KILL | Auto-send is forbidden. Paste is the product. |
| activepieces/activepieces | KILL | Same. |
| node-red/node-red | KILL | Same. |
| appsmithorg/appsmith | KILL | Internal tools we do not need. |
| supabase/supabase | HOLD | TASK-004 already blocked on Garrett's config. Do not add a second backend. |
| directus/directus | KILL | CMS sprawl. |
| posthog / metabase / plausible / matomo | KILL | No traffic to observe. |
| calcom/cal.com | KILL | Kit CTA is a phone number on a one-pager. Calendar SaaS is a week. |
| chatwoot/chatwoot | KILL | No support volume. |
| airbytehq/airbyte | KILL | Ingest for a warehouse we do not have. |
| mindsdb/mindsdb | KILL | Predictions without a series. |
| salesforce/Merlion | KILL | Time-series for $0 revenue is astrology. |
| frappe/erpnext | KILL | DirectorOS / CTS lane. Not SO. |

---

## 8. Startup-in-a-box

| Repo | Verdict | Why |
|---|---|---|
| wasp-lang/open-saas | KILL | Stripe `charges_enabled` was false 2026-08-26. |
| ixartz/SaaS-Boilerplate | KILL | Same. |
| mickasmt/next-saas-stripe-starter | KILL | Same. |
| payloadcms/payload | KILL | CMS. |
| strapi/strapi | KILL | CMS. |
| medusajs/medusa | KILL for SO | `Beexly/Clouds-bruh` already. Do not mix into Signal Origin. |

---

## 9. What we actually ship from this list

1. MCP annotations on the bus envelope (`destructiveHint` ⇒ approval).
2. Graphiti-shaped episodes for the facts that survived tonight.
3. promptfoo-shaped Pressure fixtures.
4. MetaGPT role cards as JSON, not a new crew.
5. 2D pressure field (three.js idea, canvas/SVG execution).
6. Letta split: core (desk) vs archival (origin-box). Do not host Letta.

Everything else stays in BUILD-BIBLE or the graveyard. Re-open a KILL only when it raises a dollar or a Hold score.
