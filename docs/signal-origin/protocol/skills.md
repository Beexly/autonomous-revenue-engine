# Skills — stolen from obra/superpowers, not installed

Superpowers is 286k stars of markdown that coding agents treat as mandatory. We steal three. We kill the stall.

| Skill | Ours | Not |
|---|---|---|
| test-driven-development | Failing test first. `protocol/*.test.ts`. | Their skill runner. |
| verification-before-completion | Pressure, swap, VERIFY, reset-tell. Desk QC. | A second review agent. |
| finishing-a-development-branch | Branch + PR. Never merge to main from here. | Auto-merge. |
| brainstorming | Killed as a gate. Anti-stall. Spec is the timeline and the walk-in. | "Ask what you're really trying to do" before every patch. |
| dispatching-parallel-agents | Subagents when the tasks don't share state. | A swarm runtime. |

Mastra's suspend = owner. Eliza's character file = `factory/*.json`. OpenAI handoff = `handoff.ts` → a folder on the bus.
