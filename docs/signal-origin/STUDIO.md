# STUDIO — seven rails, one OS

XXX is a rail. Not the product.

`protocol/studio.ts` is the router. Adult generate is rejected here and pointed at Beexly/XXX. Sports is rejected everywhere.

Cash this week is Kit. Everything else is a locked set waiting on a dollar we can see.

Bus envelope: `protocol/envelope.ts`. Destructive or open-world requires approval. Git is the transport. Motif QC before anything public.
