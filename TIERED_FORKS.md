# TIERED_FORKS

PLATFORM.md §5 pointed here. The file was missing. Copilot caught it.

Canonical filter: [docs/signal-origin/STACK.md](docs/signal-origin/STACK.md)

That file is steal / bible / kill against Origin constraints ($0, no publish, git is the bus). It is not a shopping list.

Copilot's "hidden problem" repos, scored the same way:

| Rec | Call |
|---|---|
| temporalio/temporal | Kill. Commits are checkpoints. Owner is HITL. |
| langfuse/langfuse | Kill as a host. Pressure + night log is the trace. |
| open-policy-agent/opa / cedar-policy/cedar | Steal as `docs/signal-origin/protocol/policy.ts`. Do not run OPA. |
| OpenLineage / Marquez | Kill. One CSV row per asset is lineage enough until a dollar hits. |
| posthog / growthbook | Kill this week. Kit conversion is a Venmo, not a funnel SDK. |
| airbytehq/airbyte | Kill. Saturday is the ingest. |

Do not add a second architecture doc. If STACK and this disagree, STACK wins.
