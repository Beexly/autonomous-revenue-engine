# SignPreview lead capture

**Until Garrett fills keys, mailto is capture.** `docs/js/lead-capture.js` opens `Baxley.Garrett@gmail.com` when `docs/js/config.js` still has placeholders. That is enough for a $150 mockup lead.

Do **not** commit Project URL or keys. Do **not** put `service_role` in `docs/`. Anon may INSERT only — see `docs/js/leads.sql` (canonical; this folder's `schema.sql` is a copy).

Resend / Edge Function notify is optional and parked. Do not buy a seat for volume we do not have.

Owner steps: KEY-REVIEW.md in `docs/signal-origin/`.
