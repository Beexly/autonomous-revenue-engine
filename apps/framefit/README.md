# FrameFit (T01)

In-browser social-frame resizer. Instasize-class one job. No server, no API key, no account.

Open `index.html` or deploy the folder as a static Vercel/GitHub Pages site.

Do not add an image model. If it needs a GPU it is a different product.
